name: ci

on:
  push:
  pull_request:

jobs:
  run-ut:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: Tests
      run: python3 waf test

